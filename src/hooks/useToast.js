import { useCallback, useEffect, useState } from 'react'

export const useToast = () => {
  const success = () => {

  }

  const error = () => {

  }

  const toast = () => {
    
  }

  return {
    default: toast,
    error,
    success
  }
}
